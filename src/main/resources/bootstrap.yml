spring:
  cloud:
    config:
      enabled: false
  data:
    mongodb:
      authentication-database: admin
      database: startup-connection
      host: localhost
      port: 27017
      auto-index-creation: true
  jpa:
    hibernate:
      ddl-auto: create-drop
server:
  servlet:
    context-path: /startup-connection
